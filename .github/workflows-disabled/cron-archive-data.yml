# Auto-generated by Session 1b cron migration.
# Replaces vercel.json entry: archive-data → 30 22 * * 6
# Invokes /api/cron/archive-data via the reusable _invoke-cron workflow.

name: "Cron · archive-data"

on:
  schedule:
    - cron: '30 22 * * 6'
  workflow_dispatch:

jobs:
  run:
    uses: ./.github/workflows/_invoke-cron.yml
    with:
      path: /api/cron/archive-data
    secrets: inherit
