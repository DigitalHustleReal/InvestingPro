# Auto-generated by Session 1b cron migration.
# Replaces vercel.json entry: check-cost-alerts → 30 6 * * *
# Invokes /api/cron/check-cost-alerts via the reusable _invoke-cron workflow.

name: "Cron · check-cost-alerts"

on:
  schedule:
    - cron: '30 6 * * *'
  workflow_dispatch:

jobs:
  run:
    uses: ./.github/workflows/_invoke-cron.yml
    with:
      path: /api/cron/check-cost-alerts
    secrets: inherit
