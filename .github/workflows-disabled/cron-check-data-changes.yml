# Auto-generated by Session 1b cron migration.
# Replaces vercel.json entry: check-data-changes → 0 19 * * *
# Invokes /api/cron/check-data-changes via the reusable _invoke-cron workflow.

name: "Cron · check-data-changes"

on:
  schedule:
    - cron: '0 19 * * *'
  workflow_dispatch:

jobs:
  run:
    uses: ./.github/workflows/_invoke-cron.yml
    with:
      path: /api/cron/check-data-changes
    secrets: inherit
