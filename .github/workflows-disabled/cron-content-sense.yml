# Auto-generated by Session 1b cron migration.
# Replaces vercel.json entry: content-sense → 0 6 * * *
# Invokes /api/cron/content-sense via the reusable _invoke-cron workflow.

name: "Cron · content-sense"

on:
  schedule:
    - cron: '0 6 * * *'
  workflow_dispatch:

jobs:
  run:
    uses: ./.github/workflows/_invoke-cron.yml
    with:
      path: /api/cron/content-sense
    secrets: inherit
