# Auto-generated by Session 1b cron migration.
# Replaces vercel.json entry: daily-revenue-report → 30 3 * * *
# Invokes /api/cron/daily-revenue-report via the reusable _invoke-cron workflow.

name: "Cron · daily-revenue-report"

on:
  schedule:
    - cron: '30 3 * * *'
  workflow_dispatch:

jobs:
  run:
    uses: ./.github/workflows/_invoke-cron.yml
    with:
      path: /api/cron/daily-revenue-report
    secrets: inherit
