# Auto-generated by Session 1b cron migration.
# Replaces vercel.json entry: sitemap-ping → 30 4 * * *
# Invokes /api/cron/sitemap-ping via the reusable _invoke-cron workflow.

name: "Cron · sitemap-ping"

on:
  schedule:
    - cron: '30 4 * * *'
  workflow_dispatch:

jobs:
  run:
    uses: ./.github/workflows/_invoke-cron.yml
    with:
      path: /api/cron/sitemap-ping
    secrets: inherit
