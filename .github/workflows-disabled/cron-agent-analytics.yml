# Auto-generated by Session 1b cron migration.
# Replaces vercel.json entry: agent-analytics → 0 22 * * *
# Invokes /api/cron/agent-analytics via the reusable _invoke-cron workflow.

name: "Cron · agent-analytics"

on:
  schedule:
    - cron: '0 22 * * *'
  workflow_dispatch:

jobs:
  run:
    uses: ./.github/workflows/_invoke-cron.yml
    with:
      path: /api/cron/agent-analytics
    secrets: inherit
