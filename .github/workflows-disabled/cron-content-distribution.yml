# Auto-generated by Session 1b cron migration.
# Replaces vercel.json entry: content-distribution → 0 15 * * *
# Invokes /api/cron/content-distribution via the reusable _invoke-cron workflow.

name: "Cron · content-distribution"

on:
  schedule:
    - cron: '0 15 * * *'
  workflow_dispatch:

jobs:
  run:
    uses: ./.github/workflows/_invoke-cron.yml
    with:
      path: /api/cron/content-distribution
    secrets: inherit
