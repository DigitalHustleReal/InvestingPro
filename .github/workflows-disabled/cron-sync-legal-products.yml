# Auto-generated by Session 1b cron migration.
# Replaces vercel.json entry: sync-legal-products → 30 0 * * *
# Invokes /api/cron/sync-legal-products via the reusable _invoke-cron workflow.

name: "Cron · sync-legal-products"

on:
  schedule:
    - cron: '30 0 * * *'
  workflow_dispatch:

jobs:
  run:
    uses: ./.github/workflows/_invoke-cron.yml
    with:
      path: /api/cron/sync-legal-products
    secrets: inherit
