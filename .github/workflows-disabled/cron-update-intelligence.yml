# Auto-generated by Session 1b cron migration.
# Replaces vercel.json entry: update-intelligence → 0 2 * * *
# Invokes /api/cron/update-intelligence via the reusable _invoke-cron workflow.

name: "Cron · update-intelligence"

on:
  schedule:
    - cron: '0 2 * * *'
  workflow_dispatch:

jobs:
  run:
    uses: ./.github/workflows/_invoke-cron.yml
    with:
      path: /api/cron/update-intelligence
    secrets: inherit
