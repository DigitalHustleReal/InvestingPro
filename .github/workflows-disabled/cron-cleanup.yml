# Auto-generated by Session 1b cron migration.
# Replaces vercel.json entry: cleanup → 30 22 * * 0
# Invokes /api/cron/cleanup via the reusable _invoke-cron workflow.

name: "Cron · cleanup"

on:
  schedule:
    - cron: '30 22 * * 0'
  workflow_dispatch:

jobs:
  run:
    uses: ./.github/workflows/_invoke-cron.yml
    with:
      path: /api/cron/cleanup
    secrets: inherit
