# Auto-generated by Session 1b cron migration.
# Replaces vercel.json entry: scrape-credit-cards → 30 20 * * 1
# Invokes /api/cron/scrape-credit-cards via the reusable _invoke-cron workflow.

name: "Cron · scrape-credit-cards"

on:
  schedule:
    - cron: '30 20 * * 1'
  workflow_dispatch:

jobs:
  run:
    uses: ./.github/workflows/_invoke-cron.yml
    with:
      path: /api/cron/scrape-credit-cards
    secrets: inherit
