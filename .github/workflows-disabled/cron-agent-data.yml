# Auto-generated by Session 1b cron migration.
# Replaces vercel.json entry: agent-data → 30 19 * * *
# Invokes /api/cron/agent-data via the reusable _invoke-cron workflow.

name: "Cron · agent-data"

on:
  schedule:
    - cron: '30 19 * * *'
  workflow_dispatch:

jobs:
  run:
    uses: ./.github/workflows/_invoke-cron.yml
    with:
      path: /api/cron/agent-data
    secrets: inherit
