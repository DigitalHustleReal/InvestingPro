# Auto-generated by Session 1b cron migration.
# Replaces vercel.json entry: analytics-sync → 0 16 * * *
# Invokes /api/cron/analytics-sync via the reusable _invoke-cron workflow.

name: "Cron · analytics-sync"

on:
  schedule:
    - cron: '0 16 * * *'
  workflow_dispatch:

jobs:
  run:
    uses: ./.github/workflows/_invoke-cron.yml
    with:
      path: /api/cron/analytics-sync
    secrets: inherit
