# Auto-generated by Session 1b cron migration.
# Replaces vercel.json entry: agent-content-scout → 30 20 * * *
# Invokes /api/cron/agent-content-scout via the reusable _invoke-cron workflow.

name: "Cron · agent-content-scout"

on:
  schedule:
    - cron: '30 20 * * *'
  workflow_dispatch:

jobs:
  run:
    uses: ./.github/workflows/_invoke-cron.yml
    with:
      path: /api/cron/agent-content-scout
    secrets: inherit
