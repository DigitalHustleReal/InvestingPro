# Auto-generated by Session 1b cron migration.
# Replaces vercel.json entry: sync-amfi-data → 30 2 * * *
# Invokes /api/cron/sync-amfi-data via the reusable _invoke-cron workflow.

name: "Cron · sync-amfi-data"

on:
  schedule:
    - cron: '30 2 * * *'
  workflow_dispatch:

jobs:
  run:
    uses: ./.github/workflows/_invoke-cron.yml
    with:
      path: /api/cron/sync-amfi-data
    secrets: inherit
