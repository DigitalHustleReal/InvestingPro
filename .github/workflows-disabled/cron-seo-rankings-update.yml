# Auto-generated by Session 1b cron migration.
# Replaces vercel.json entry: seo-rankings-update → 30 5 * * *
# Invokes /api/cron/seo-rankings-update via the reusable _invoke-cron workflow.

name: "Cron · seo-rankings-update"

on:
  schedule:
    - cron: '30 5 * * *'
  workflow_dispatch:

jobs:
  run:
    uses: ./.github/workflows/_invoke-cron.yml
    with:
      path: /api/cron/seo-rankings-update
    secrets: inherit
