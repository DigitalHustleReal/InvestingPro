# Auto-generated by Session 1b cron migration.
# Replaces vercel.json entry: email-sequences → 0 14 * * *
# Invokes /api/cron/email-sequences via the reusable _invoke-cron workflow.

name: "Cron · email-sequences"

on:
  schedule:
    - cron: '0 14 * * *'
  workflow_dispatch:

jobs:
  run:
    uses: ./.github/workflows/_invoke-cron.yml
    with:
      path: /api/cron/email-sequences
    secrets: inherit
