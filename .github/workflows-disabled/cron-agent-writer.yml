# Auto-generated by Session 1b cron migration.
# Replaces vercel.json entry: agent-writer → 0 1 * * *
# Invokes /api/cron/agent-writer via the reusable _invoke-cron workflow.

name: "Cron · agent-writer"

on:
  schedule:
    - cron: '0 1 * * *'
  workflow_dispatch:

jobs:
  run:
    uses: ./.github/workflows/_invoke-cron.yml
    with:
      path: /api/cron/agent-writer
    secrets: inherit
