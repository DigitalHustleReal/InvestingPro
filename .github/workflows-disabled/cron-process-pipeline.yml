# Auto-generated by Session 1b cron migration.
# Replaces vercel.json entry: process-pipeline → 0 17 * * *
# Invokes /api/cron/process-pipeline via the reusable _invoke-cron workflow.

name: "Cron · process-pipeline"

on:
  schedule:
    - cron: '0 17 * * *'
  workflow_dispatch:

jobs:
  run:
    uses: ./.github/workflows/_invoke-cron.yml
    with:
      path: /api/cron/process-pipeline
    secrets: inherit
