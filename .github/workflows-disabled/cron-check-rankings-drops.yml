# Auto-generated by Session 1b cron migration.
# Replaces vercel.json entry: check-rankings-drops → 30 0 * * *
# Invokes /api/cron/check-rankings-drops via the reusable _invoke-cron workflow.

name: "Cron · check-rankings-drops"

on:
  schedule:
    - cron: '30 0 * * *'
  workflow_dispatch:

jobs:
  run:
    uses: ./.github/workflows/_invoke-cron.yml
    with:
      path: /api/cron/check-rankings-drops
    secrets: inherit
