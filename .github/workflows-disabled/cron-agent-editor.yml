# Auto-generated by Session 1b cron migration.
# Replaces vercel.json entry: agent-editor → 30 2 * * *
# Invokes /api/cron/agent-editor via the reusable _invoke-cron workflow.

name: "Cron · agent-editor"

on:
  schedule:
    - cron: '30 2 * * *'
  workflow_dispatch:

jobs:
  run:
    uses: ./.github/workflows/_invoke-cron.yml
    with:
      path: /api/cron/agent-editor
    secrets: inherit
