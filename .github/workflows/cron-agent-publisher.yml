# Auto-generated by Session 1b cron migration.
# Replaces vercel.json entry: agent-publisher → 30 4 * * *
# Invokes /api/cron/agent-publisher via the reusable _invoke-cron workflow.

name: "Cron · agent-publisher"

on:
  schedule:
    - cron: '30 4 * * *'
  workflow_dispatch:

jobs:
  run:
    uses: ./.github/workflows/_invoke-cron.yml
    with:
      path: /api/cron/agent-publisher
    secrets: inherit
