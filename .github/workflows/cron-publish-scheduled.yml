# Auto-generated by Session 1b cron migration.
# Replaces vercel.json entry: publish-scheduled → 0 13 * * *
# Invokes /api/cron/publish-scheduled via the reusable _invoke-cron workflow.

name: "Cron · publish-scheduled"

on:
  schedule:
    - cron: '0 13 * * *'
  workflow_dispatch:

jobs:
  run:
    uses: ./.github/workflows/_invoke-cron.yml
    with:
      path: /api/cron/publish-scheduled
    secrets: inherit
