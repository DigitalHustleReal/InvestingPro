# Auto-generated by Session 1b cron migration.
# Replaces vercel.json entry: check-links → 30 18 * * 0
# Invokes /api/cron/check-links via the reusable _invoke-cron workflow.

name: "Cron · check-links"

on:
  schedule:
    - cron: '30 18 * * 0'
  workflow_dispatch:

jobs:
  run:
    uses: ./.github/workflows/_invoke-cron.yml
    with:
      path: /api/cron/check-links
    secrets: inherit
