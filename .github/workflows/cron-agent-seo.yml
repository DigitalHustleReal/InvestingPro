# Auto-generated by Session 1b cron migration.
# Replaces vercel.json entry: agent-seo → 30 0 * * *
# Invokes /api/cron/agent-seo via the reusable _invoke-cron workflow.

name: "Cron · agent-seo"

on:
  schedule:
    - cron: '30 0 * * *'
  workflow_dispatch:

jobs:
  run:
    uses: ./.github/workflows/_invoke-cron.yml
    with:
      path: /api/cron/agent-seo
    secrets: inherit
