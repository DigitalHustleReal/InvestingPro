# Auto-generated by Session 1b cron migration.
# Replaces vercel.json entry: daily-content-generation → 30 20 * * *
# Invokes /api/cron/daily-content-generation via the reusable _invoke-cron workflow.

name: "Cron · daily-content-generation"

on:
  schedule:
    - cron: '30 20 * * *'
  workflow_dispatch:

jobs:
  run:
    uses: ./.github/workflows/_invoke-cron.yml
    with:
      path: /api/cron/daily-content-generation
    secrets: inherit
