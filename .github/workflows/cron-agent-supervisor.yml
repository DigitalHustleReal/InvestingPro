# Auto-generated by Session 1b cron migration.
# Replaces vercel.json entry: agent-supervisor → 0 0 * * *
# Invokes /api/cron/agent-supervisor via the reusable _invoke-cron workflow.

name: "Cron · agent-supervisor"

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  run:
    uses: ./.github/workflows/_invoke-cron.yml
    with:
      path: /api/cron/agent-supervisor
    secrets: inherit
