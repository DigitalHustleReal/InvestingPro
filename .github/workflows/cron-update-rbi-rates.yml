# Auto-generated by Session 1b cron migration.
# Replaces vercel.json entry: update-rbi-rates → 30 1 * * *
# Invokes /api/cron/update-rbi-rates via the reusable _invoke-cron workflow.

name: "Cron · update-rbi-rates"

on:
  schedule:
    - cron: '30 1 * * *'
  workflow_dispatch:

jobs:
  run:
    uses: ./.github/workflows/_invoke-cron.yml
    with:
      path: /api/cron/update-rbi-rates
    secrets: inherit
