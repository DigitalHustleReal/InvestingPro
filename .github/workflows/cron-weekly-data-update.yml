# Auto-generated by Session 1b cron migration.
# Replaces vercel.json entry: weekly-data-update → 30 20 * * 6
# Invokes /api/cron/weekly-data-update via the reusable _invoke-cron workflow.

name: "Cron · weekly-data-update"

on:
  schedule:
    - cron: '30 20 * * 6'
  workflow_dispatch:

jobs:
  run:
    uses: ./.github/workflows/_invoke-cron.yml
    with:
      path: /api/cron/weekly-data-update
    secrets: inherit
