# Auto-generated by Session 1b cron migration.
# Replaces vercel.json entry: check-data-freshness → 0 12 * * *
# Invokes /api/cron/check-data-freshness via the reusable _invoke-cron workflow.

name: "Cron · check-data-freshness"

on:
  schedule:
    - cron: '0 12 * * *'
  workflow_dispatch:

jobs:
  run:
    uses: ./.github/workflows/_invoke-cron.yml
    with:
      path: /api/cron/check-data-freshness
    secrets: inherit
