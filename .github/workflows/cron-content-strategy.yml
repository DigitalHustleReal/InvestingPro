# Auto-generated by Session 1b cron migration.
# Replaces vercel.json entry: content-strategy → 30 21 * * *
# Invokes /api/cron/content-strategy via the reusable _invoke-cron workflow.

name: "Cron · content-strategy"

on:
  schedule:
    - cron: '30 21 * * *'
  workflow_dispatch:

jobs:
  run:
    uses: ./.github/workflows/_invoke-cron.yml
    with:
      path: /api/cron/content-strategy
    secrets: inherit
