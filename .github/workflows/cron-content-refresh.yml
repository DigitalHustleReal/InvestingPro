# Auto-generated by Session 1b cron migration.
# Replaces vercel.json entry: content-refresh → 30 11 * * *
# Invokes /api/cron/content-refresh via the reusable _invoke-cron workflow.

name: "Cron · content-refresh"

on:
  schedule:
    - cron: '30 11 * * *'
  workflow_dispatch:

jobs:
  run:
    uses: ./.github/workflows/_invoke-cron.yml
    with:
      path: /api/cron/content-refresh
    secrets: inherit
