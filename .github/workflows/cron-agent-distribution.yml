# Auto-generated by Session 1b cron migration.
# Replaces vercel.json entry: agent-distribution → 0 5 * * *
# Invokes /api/cron/agent-distribution via the reusable _invoke-cron workflow.

name: "Cron · agent-distribution"

on:
  schedule:
    - cron: '0 5 * * *'
  workflow_dispatch:

jobs:
  run:
    uses: ./.github/workflows/_invoke-cron.yml
    with:
      path: /api/cron/agent-distribution
    secrets: inherit
