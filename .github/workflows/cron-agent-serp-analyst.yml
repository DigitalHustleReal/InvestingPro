# Auto-generated by Session 1b cron migration.
# Replaces vercel.json entry: agent-serp-analyst → 30 21 * * *
# Invokes /api/cron/agent-serp-analyst via the reusable _invoke-cron workflow.

name: "Cron · agent-serp-analyst"

on:
  schedule:
    - cron: '30 21 * * *'
  workflow_dispatch:

jobs:
  run:
    uses: ./.github/workflows/_invoke-cron.yml
    with:
      path: /api/cron/agent-serp-analyst
    secrets: inherit
