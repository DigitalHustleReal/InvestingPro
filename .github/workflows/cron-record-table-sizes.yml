# Auto-generated by Session 1b cron migration.
# Replaces vercel.json entry: record-table-sizes → 30 8 * * *
# Invokes /api/cron/record-table-sizes via the reusable _invoke-cron workflow.

name: "Cron · record-table-sizes"

on:
  schedule:
    - cron: '30 8 * * *'
  workflow_dispatch:

jobs:
  run:
    uses: ./.github/workflows/_invoke-cron.yml
    with:
      path: /api/cron/record-table-sizes
    secrets: inherit
