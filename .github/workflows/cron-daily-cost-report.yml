# Auto-generated by Session 1b cron migration.
# Replaces vercel.json entry: daily-cost-report → 30 7 * * *
# Invokes /api/cron/daily-cost-report via the reusable _invoke-cron workflow.

name: "Cron · daily-cost-report"

on:
  schedule:
    - cron: '30 7 * * *'
  workflow_dispatch:

jobs:
  run:
    uses: ./.github/workflows/_invoke-cron.yml
    with:
      path: /api/cron/daily-cost-report
    secrets: inherit
