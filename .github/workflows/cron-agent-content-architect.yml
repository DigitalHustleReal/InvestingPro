# Auto-generated by Session 1b cron migration.
# Replaces vercel.json entry: agent-content-architect → 30 22 * * *
# Invokes /api/cron/agent-content-architect via the reusable _invoke-cron workflow.

name: "Cron · agent-content-architect"

on:
  schedule:
    - cron: '30 22 * * *'
  workflow_dispatch:

jobs:
  run:
    uses: ./.github/workflows/_invoke-cron.yml
    with:
      path: /api/cron/agent-content-architect
    secrets: inherit
