# Auto-generated by Session 1b cron migration.
# Replaces vercel.json entry: sync-rankings → 30 10 * * *
# Invokes /api/cron/sync-rankings via the reusable _invoke-cron workflow.

name: "Cron · sync-rankings"

on:
  schedule:
    - cron: '30 10 * * *'
  workflow_dispatch:

jobs:
  run:
    uses: ./.github/workflows/_invoke-cron.yml
    with:
      path: /api/cron/sync-rankings
    secrets: inherit
