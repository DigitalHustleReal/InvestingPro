# Auto-generated by Session 1b cron migration.
# Replaces vercel.json entry: generate-missing-images → 30 9 * * *
# Invokes /api/cron/generate-missing-images via the reusable _invoke-cron workflow.

name: "Cron · generate-missing-images"

on:
  schedule:
    - cron: '30 9 * * *'
  workflow_dispatch:

jobs:
  run:
    uses: ./.github/workflows/_invoke-cron.yml
    with:
      path: /api/cron/generate-missing-images
    secrets: inherit
